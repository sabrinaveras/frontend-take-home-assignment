import * as React from 'react';

// styles
import * as Styled from './home.styles';

const HomePage: React.FunctionComponent = () => {
  return <Styled.Container>HomePage</Styled.Container>;
};

export default HomePage;
